<div class="banner_cov">
 <%= image_tag("banner_inn.jpg") %></div>  
 <div class="container">
 <div class="row">
  <div class="col-md-6 sign_pop col-md-offset-3">
<p id="notice"><%= notice %></p>
<div class="job_bx">
<h1>
  <strong>Title:</strong>
  <%= @resume.title %>
</h1>

<p>
  <strong>Description:</strong>
  <%= @resume.description %>
</p>
<p>
  <strong>CV:</strong>
  <!-- <iframe src="<%= @resume.cv.url%>"></iframe> -->
  <%= link_to @resume.cv_file_name, download_resume_path(@resume),  target: "_blank"%>

</p>

<%= link_to 'Edit', edit_resume_path(@resume) if current_user.candidate? %> | 
<%= link_to 'Back', users_dashboard_path %>
</div></div></div></div>
<%= form_for :interview,url: job_interviews_path(@job) do |f|%>

	<%= f.label :stage %>
	<br/>
	<%= f.select(:total_stage, [["Stage 1", 1], ["Stage 2",2], ["Stage 3", 3], ["Stage 4",4],["Stage 5", 5], ["Stage 6",6], ["Stage 7", 7], ["Stage 8", 8], ["Stage 9", 9], ["Stage 10", 10]]) %>
	<%= f.submit :save , style: "float:right;"%>
<% end %>
